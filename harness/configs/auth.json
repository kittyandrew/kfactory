{
  "_comment": "Pre-staged bogus auth for the harness. ensureFresh sees the far-future expires_at, short-circuits the OIDC refresh path, sends 'harness-fake' as the Bearer. The opencode server (no proxy, no JWT validator) accepts any bearer. NEVER use this shape in production -- the harness's whole point is to bypass OIDC for E2E testing.",
  "schema_version": 1,
  "server": "http://kfactory-opencode:4096",
  "issuer": "http://harness-fake-idp",
  "client_id": "harness-fake-client",
  "audience": "harness-fake-audience",
  "access_token": "harness-fake-bearer",
  "refresh_token": "harness-fake-refresh",
  "expires_at": "2099-01-01T00:00:00Z"
}
