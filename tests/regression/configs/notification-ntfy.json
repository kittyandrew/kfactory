{
  "_comment": [
    "ntfy plugin config for the regression tests. The 'server' is the regression tests'",
    "ntfy container reachable on the Docker bridge by name; 'topic' matches",
    "what the operator opens in the host browser at http://localhost:8080/<topic>.",
    "notifyAfter uses shorthand duration ('3s' = 3 seconds) so the regression tests",
    "loop can validate the wait window quickly. fetchTimeout same shape."
  ],
  "enabled": true,
  "events": {
    "session.idle": {
      "enabled": true,
      "notifyAfter": "3s"
    },
    "session.error": {
      "enabled": true,
      "notifyAfter": "0s"
    },
    "permission.asked": {
      "enabled": true,
      "notifyAfter": "0s"
    }
  },
  "backend": {
    "topic": "kfactory-regression",
    "server": "http://kfactory-ntfy:80",
    "priority": "default",
    "fetchTimeout": "5s"
  }
}
