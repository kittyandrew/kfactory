{
  "_comment": "Pre-staged bogus auth for the regression tests. ensureFresh sees the far-future expires_at, short-circuits the OIDC refresh path, sends 'regression-fake' as the Bearer. The opencode server (no proxy, no JWT validator) accepts any bearer. NEVER use this shape in production -- the regression tests' whole point is to bypass OIDC for regression testing.",
  "schema_version": 1,
  "server": "http://kfactory-opencode:4096",
  "issuer": "http://regression-fake-idp",
  "client_id": "regression-fake-client",
  "audience": "regression-fake-audience",
  "access_token": "regression-fake-bearer",
  "refresh_token": "regression-fake-refresh",
  "expires_at": "2099-01-01T00:00:00Z"
}
